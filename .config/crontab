# After making changes to this file, reinstall using command:
# setup-cron

PATH=$HOME/bin:$HOME/.local/bin:/opt/homebrew/bin:/home/linuxbrew/.linuxbrew/bin:/usr/local/bin:/usr/bin:/bin

*/10 * * * * cron-run sync-work-time $HOME/bin/sync-work-time
0 */6 * * * cron-run aw-export-snapshot $HOME/bin/aw-export-snapshot

# Moved to launchd (~/.config/launchd/, installed via setup-launchd):
#   crm-sync-bm  — cron can't read ~/Documents (macOS TCC); find returned 0 and zeroed the goals.
#   backup-urls  — weekly 3am job that cron skips while asleep; launchd reruns it on wake.
