#!/usr/bin/env bash
# Usage: cron-run <logname> <command> [args...]
LOG_FILE="$HOME/.local/log/$1.log"
mkdir -p "$(dirname "$LOG_FILE")"
shift
"$@" 2>&1 | while IFS= read -r line; do
    printf '[%s] %s\n' "$(date '+%Y-%m-%d %H:%M:%S')" "$line"
done >> "$LOG_FILE"
